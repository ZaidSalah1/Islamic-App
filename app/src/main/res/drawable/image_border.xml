<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="60dp" /> <!-- Adjust the radius as needed -->
    <solid android:color="@android:color/white" /> <!-- Set the background color -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white"/><!-- Set the border width -->
</shape>
